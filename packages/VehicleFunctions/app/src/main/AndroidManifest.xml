<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.volvocars.vehiclefunctions">
    <uses-permission android:name="android.car.permission.CAR_VENDOR_EXTENSION"/>
    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="27"></uses-sdk>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/VolvoTheme.VehicleFunction">
        <activity android:name="com.volvocars.vehiclefunctions.VehicleFunctionsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>