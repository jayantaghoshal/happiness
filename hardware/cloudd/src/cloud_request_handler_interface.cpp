/*
 * A file which includes the header file cloud_request_handler_interface.h in order
 * to ensure, at compile time, that there are no missing dependencies in the header file.
 */

#include "cloud_request_handler_interface.h"