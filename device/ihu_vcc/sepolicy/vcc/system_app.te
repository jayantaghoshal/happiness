# system apps
allow system_app carconfig_prop:file { getattr open read };
#hwbinder_use(system_app)

# Allow IIplm HAL client
hal_client_domain(system_app, hal_iplm)

# Allow ILocationUpdate HAL client
hal_client_domain(system_app, hal_gps)

# Allow IRemoteCtrlPropertyResponse HAL client
hal_client_domain(system_app, hal_remotectrl)
