<resources>
    <!-- This string array should be overridden by the device to present a list of network
         attributes.  This is used by the connectivity manager to decide which networks can coexist
         based on the hardware -->
    <!-- An Array of "[Connection name],[ConnectivityManager.TYPE_xxxx],
         [associated radio-type],[priority],[restoral-timer(ms)],[dependencyMet]  -->
    <!-- the 5th element "restore-time" indicates the number of milliseconds to delay
         before automatically restore the default connection.  Set -1 if the connection
         does not require auto-restore. -->
    <!-- the 6th element indicates boot-time dependency-met value. -->
    <string-array translatable="false" name="networkAttributes">
        <item>"bluetooth,7,7,2,60000,true"</item>
        <item>"ethernet,9,9,2,60000,true"</item>
    </string-array>

    <!-- This string array should be overridden by the device to present a list of radio
         attributes.  This is used by the connectivity manager to decide which networks can coexist
         based on the hardware -->
    <!-- An Array of "[ConnectivityManager connectionType],
                               [# simultaneous connection types]"  -->
    <string-array translatable="false" name="radioAttributes">
        <item>"7,1"</item>
        <item>"9,1"</item>
    </string-array>

    <!-- Minimum screen brightness setting allowed by the power manager.
        The user is forbidden from setting the brightness below this level.
        Note: Modifed due to volvo requirements -->
    <integer name="config_screenBrightnessSettingMinimum">1</integer>

    <!-- Specifies the path that is used by AdaptiveIconDrawable class to crop launcher icons. -->
    <string name="config_icon_mask" translatable="false">"M0,77 L0,23 Q0,0 23,0 L77,0 Q100,0 100,23 L100,77 Q100,100 77,100 L23,100 Q0,100 0,77Z"</string>

    <!--Specifies that Target have multi-window/screen support.
        This setting is enabled as default on phones but disabled for Car, it is however needed for proper DIM/cluster support -->
    <bool name="config_supportsMultiWindow">true</bool>

    <!-- Configuration of Ethernet interfaces in the following format:
    <interface name|mac address>;[Network Capabilities];[IP config]
    Where
        [Network Capabilities] Optional. A comma seprated list of network capabilities.
            Values must be from NetworkCapabilities#NET_CAPABILITIES_* constants.
        [IP config] Optional. If empty or not specified - DHCP will be used, otherwise
            static IP address with the mask.
    -->
    <string-array translatable="false" name="config_ethernet_interfaces">

        <!-- Interface eth0

            Default network interface which only becomes active when a usb4ethernet device
            is connected.
        -->
        <item>eth0;12,13,14,15;</item>

        <!-- Interface eth1

            A virtual network interface which bridges the default netns and VCC netns.
            Interface is reserved for RTP audio streaming for the convinience API.
        -->
        <item>eth1;13,14,15;ip=10.0.0.2/28 gateway=10.0.0.1</item>


    </string-array>
</resources>

