package vendor.volvocars.hardware.settings@1.0;

import vendor.volvocars.hardware.profiles@1.0::types;

interface ISettingsListener 
{    
    settingsForCurrentUserChanged(uint32_t key, SettingsChangeReason reason, ProfileIdentifier currentProfile);
};
